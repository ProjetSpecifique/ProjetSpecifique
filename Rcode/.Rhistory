dir()
"../CSVFiles/Tamura.csv""../CSVFiles/Tamura.csv..VTerms/
desc <- read.csv("../CSVFiles/Tamura.csv", stringsAsFactors=F, head=F, sep=",")
terms <- read.csv("../CSVTerms/sky.csv", stringsAsFactors=F, head=F, sep=";")
colnames(terms) <- c("photoID", "class")
colnames(desc) <- c("photoName")
f <- function(x, y) merge(x, y[grepl(x$photoID[1], y$photoName),], all=F)
res <- do.call(rbind, by(desc, desc$photoName, f, terms))
f <- function(x, y) merge(x, y[grepl(y$photoID[1], x$photoName),], all=F)
res <- do.call(rbind, by(desc, desc$photoName, f, terms))
f <- function(x, y) merge(x, y[grepl(x$photoID[1], y$photoName),], all=F)
res <- do.call(rbind, by(terms, terms$photoID, f, desc))
q()
